import { memberRepository } from '../../../repository';
import { CharacterView } from '../../../types/view';
import { useMemberState } from '../../../useState';

export { useQueryLogic as useMemberSearchQueryLogic };

const useQueryLogic = () => {
  const characterList = undefined;

  return {
    characterList
  };
};
